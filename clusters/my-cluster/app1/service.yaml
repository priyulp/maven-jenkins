apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
  namespace: default
spec:
  selector:
    app: tomcat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
  
  type: NodePort
ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
  
